# Core dependencies
audioop-lts>=0.2.1; python_version >= "3.13"  # audioop compatibility for Python 3.13+
pydub>=0.25.0          # Audio processing
pyyaml>=6.0            # Config parsing
click>=8.0             # CLI framework
soundfile>=0.12.0      # Audio file I/O
numpy>=1.24.0          # Array operations

# TTS Engines
edge-tts>=6.1.0        # Edge TTS (cloud, free)
# kokoro-onnx>=0.4.0   # Kokoro-ONNX TTS (local) - uncomment if using local TTS
